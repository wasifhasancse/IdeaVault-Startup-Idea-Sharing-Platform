export const metadata = {
  title: "Edit Profile",
  description:
    "Update your IdeaVault profile information, avatar, and account details.",
};

export default function EditProfileLayout({ children }) {
  return children;
}
